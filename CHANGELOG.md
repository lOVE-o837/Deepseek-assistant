# 更新日志 (CHANGELOG)

所有值得关注的变更都会记录在此文件中。格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。


## [1.5.0] - 2026-08-05

### ✨ 新增
- **批量操作系统**：收藏夹、历史搜索、对话消息标签页全面支持多选操作。新增 SelectionState 状态管理器，基于 Set 管理选中状态，与 DOM 完全解耦。支持全选/取消全选、Shift 连续选择、按"全部/仅对话/仅消息"分类全选。底部浮动批量操作栏提供批量删除、批量移动、批量导出、批量收藏、批量备注（覆盖/追加两种模式）功能。
- **四角星 ✴ 格式块独立收藏**：DeepSeek 页面中的代码块、表格、数学公式、Mermaid 图表旁自动注入金色四角星按钮，点击可独立收藏单个格式块。代码块保留围栏标记和语言标识，表格从 HTML 重建标准 Markdown 表格，数学公式提取 KaTeX annotation 源码。支持基于指纹的精确去重，刷新页面后按钮保持金色正方形高亮。AI 回答思考区域内不注入按钮。
- **对话消息 API 全量获取**：对话消息标签页数据源从 DOM 虚拟滚动抓取（仅 5-8 条）升级为 API 全量获取。通过 Content Script 代理 API 请求绕过跨域限制，支持 get-auth-info 和 fetch-history-messages 消息路由。默认显示 AI 回答完整 Markdown 内容，支持格式块类型过滤（代码块/表格/数学公式/Mermaid）、用户提问独立过滤。增加内容纯度检测，智能过滤被误判为公式/表格的碎片文本。用户提问不拆分，保留完整纯文本。正序/倒序保持用户提问→AI 回答的轮次结构。
- **消息预览浮层**：对话搜索和收藏夹标签页均支持鼠标悬停 0.4 秒弹出预览浮层。引入 marked.js + highlight.js + KaTeX 实现代码语法高亮和数学公式可视化渲染。浮层采用智能位置自适应（下方优先、空间不足翻转、右侧溢出左偏移）、单浮层 DOM 复用。配备滑动开关独立控制两个标签页，状态持久化。鼠标离开弹窗或消息项即时消失，滚动/过滤/切换模式时强制隐藏。
- **五角星 ⭐ 收藏 API 原始 Markdown**：收藏整轮 AI 回答时，优先通过 API 获取完整原始 Markdown 替换 DOM 纯文本。通过内容相似度 + 角色校验的评分机制精准定位目标消息，索引偏移 ±3 范围搜索。导出 Markdown 时完美还原表格/代码块/公式原始格式。
- **历史搜索 API 全量获取**：历史搜索从 DOM 模拟虚拟滚动升级为 API 分页全量获取（/chat_session/fetch_page），秒加载全部对话。实现 API 优先 + DOM 降级双层策略，侧边栏折叠时自动降级并弹出提示。显示精确时间戳（今天 14:32 / 昨天 09:15 / 3天前 / 07-20 周三 / 2025-03-15 周六），支持中英文自动切换。
- **格式块跳转导航**：新增 Ctrl+↑/↓ 快捷键在代码块、表格、标题等格式块之间快速跳转。悬浮按钮长按菜单可选择优先跳转类型。跳转后红色高亮 1 秒自动消失，快速连续跳转无残留。新用户首次使用时弹出提示弹窗，支持"不再提示"勾选。
- **面板拖拽缩放**：面板右边缘（340~900px）、下边缘（400~1200px）、右下角同步缩放。手柄在父页面 DOM 中 requestAnimationFrame 持续同步，拖拽丝滑无延迟，尺寸记忆持久化到 chrome.storage.local。
- **弹窗优先级队列**：新增弹窗队列管理器（enqueuePopup / dequeuePopup / processPopupQueue），版本更新弹窗优先级 1（最高），格式块提示弹窗优先级 2。Canvas Modal 增加 onClose 回调支持，弹窗关闭后自动触发队列中下一个弹窗。
- **收藏夹对话框片折叠开关**：折叠/展开切换，状态持久化到 chrome.storage.local，国际化跟随浏览器语言。
- **收藏夹全选模式下拉**：支持"全部/仅对话/仅消息"三种模式切换，状态持久化，模式切换时同步更新选中状态。
- **导出内容清洗**：导出时自动移除 BOM 头、统一换行符为 LF、移除零宽字符。格式块前后自动增加空行，表格缺失分隔行时智能补充。

### 🔧 优化
- 对话搜索标签页全选操作采用分批异步更新 DOM（每批 300 个），消除浏览器崩溃风险。
- 标签栏切换过渡动画移除（transition: none），数据加载延迟执行不阻塞 UI。
- 对话搜索块类型筛选互斥规则完善，勾选纯文本时强制取消全部和所有格式块，优先级链：纯文本 > 格式块 > 全部。
- 操作栏按钮文字精简（批量导出→导出、移动到…→移动），操作栏挂载到 tab-bookmarks 底部避免截断。
- 收藏夹对话/消息区域按比例显示（#bookmark-list-wrapper 限制 max-height: 40%）。
- 预览浮层开关采用滑动设计，圆点大小与开关条高度一致。
- 格式块按钮位置差异化适配（代码块挂条幅右上角外侧、表格/公式挂容器右上角）。
- 四角星/五角星按钮持久高亮采用 CSS !important + JS 内联样式双重兜底。
- 对话框片统一只显示对话名称，快捷键收藏和搜索收藏行为一致。
- 旧版 DOM 搜索代码彻底清理（executeCurrentSearch、renderSearchResults 等），减少维护负担。
- 翻译键新增 70+（批量操作、预览、格式块过滤、折叠、全选模式等），国际化覆盖更全面。

### 🐛 修复
- 修复 3200+ 条消息全选时浏览器崩溃问题。
- 修复全选后立即滚动偶发卡顿（1413 条消息时 1-2 秒轻微卡顿后流畅）。
- 修复数学公式按钮偶发消失（改用 .katex-html 选择器，挂载到非内联祖先）。
- 修复表格导出只显示表头（增强正则完整匹配多行表格含空单元格）。
- 修复用户提问在对话搜索中重复显示（parseMessageBlocks 中纯文本被添加两次）。
- 修复弹窗优先级竞争：版本更新弹窗和格式块提示弹窗同时出现（队列机制 + 延迟 1500ms 触发）。
- 修复格式块跳转红色高亮不消失（1 秒定时器完整清除边框/阴影/背景色）。
- 修复底部向上跳转卡住（回滚强制清除 lastActiveBlock 的代码）。
- 修复折叠按钮和全选下拉初次语言错误（初始化增加 navigator.language 回退，延迟 50ms 确保 t() 就绪）。
- 修复 highlight.js 安全警告（完全依赖 marked 的 highlight 回调安全转义）。
- 修复 KaTeX 特殊字符警告（预处理替换 Unicode 字符 + strict: "ignore" 最高容错）。
- 修复代码块预览字体模糊（弹窗 position: fixed + -webkit-font-smoothing: antialiased）。
- 修复收藏夹统计文件夹筛选不同步（统一使用 refreshBookmarkStats）。
- 修复 DataCloneError（showFeedbackPopupV2 的 buttonAction 从函数改为 URL 字符串）。
- 修复消息统计残留（删除 #message-bookmark-count 的 HTML 占位符和 CSS 样式）。
- 修复语言切换后对话搜索统计数字不更新（switchLanguage 末尾延迟执行 applyMessagesData）。
- 修复导出遗漏消息收藏（删除冗余 exportAsMarkdown 函数，统一使用 exportAsMarkdownBatch）。
- 修复 #bookmark-list-wrapper 布局导致界面比例失调。

## [1.4.1] - 2026-06-06

### ✨ 新增
- **用户反馈渠道**：首次打开面板的友好提示及设置页固定入口，帮助项目收集使用需求。
- **独家主题特效**：新增隐藏款“深海极客”主题，包含动态光晕、流光边框与粒子背景，可通过特定互动方式解锁。
- 设置页新增“专属奖励”兑换功能，配合社区活动使用。
- 设置页收藏统计卡片动态显示当前文件夹名称。

### 🔧 优化
- 初次安装或重置后，面板立即呈现默认深海蓝主题，不再短暂显示白色。
- 浅色主题下（深海蓝、极简白、护眼绿、暖光橙）导航栏文字更清晰。
- 面板顶部空白区域移除，界面更紧凑。
- 隐藏主题特效仅在该主题下显示，切换后自动消失。

### 🐛 修复
- 修复删除收藏后悬浮按钮未实时恢复默认状态的问题。
- 修复切换回其他主题后，深色背景或特效残留的问题。
- 修复隐藏主题下文字模糊、导航栏文字不可见等问题。
- 修复粒子特效在隐藏主题下丢失的显示错误。
- 修复主题切换后面板出现多余滚动条的问题。
- 清理了重复的导航栏样式规则，提升可维护性。

## [1.4.0] - 2026-06-04

### 🏗️ 架构重构
- **Content Script 模块化拆分**：将约 2000 行 `content.js` 拆分为 7 个独立模块（utils, panel, bookmark, float-btn, sidebar, search, core），通过 `window.__DS__` 命名空间通信，各模块可独立维护。

### ✨ 优化
- **样式统一**：「移动到文件夹」按钮改为绿色风格，与打开/备注（蓝色）、删除（红色）保持一致，hover 均有颜色反转反馈。
- **统计增强**：设置页收藏统计卡片动态显示当前选中的文件夹名称，随文件夹切换实时更新。
- **Content Script 国际化**：长按菜单、收藏弹窗、消息提示等页面端 UI 全部支持中英文切换（使用 `lang` 变量 + 三元表达式，绕过面板 `t()` 函数上下文隔离限制）。
- **空状态显示修复**：收藏夹和消息收藏空状态改用三元表达式直接判断语言，彻底解决 `I18N` 初始化时序导致的显示键名问题（`bookmarksEmpty` 等）。
- **Markdown 导出增强**：导出时完整保留 AI 代码块（自动识别语言并添加 Markdown 代码围栏）。
- **语言切换同步**：`content-core.js` 中 `lang` 变量随 `settings-changed` 消息同步更新，确保页面端 UI 实时切换语言。
- **字体渲染优化**：Windows ClearType 原生渲染，彻底解决笔画重叠黑块问题。
- **存储百分比精确化**：小数点后两位精确显示。
- **Toast 提示完善**：语言切换成功提示、收藏/删除/定位/超时等 20+ 处 Toast 国际化遗漏全部补全。
- **备注编辑弹窗国际化**：`prompt()` 弹窗根据当前语言显示中英文提示。

### 🐛 修复
- 修复删除收藏后右下角悬浮按钮无法即时恢复默认外观的问题，现在按钮状态实时同步。
- 修复恢复默认设置按钮无反应（`computeThemeCSSVars` 遗漏迁移到 `module-theme.js`）。
- 修复当前对话搜索角色标签（AI回复/用户消息）语言切换不更新（`content.js` 硬编码源头修复）。
- 修复搜索排序按钮初次显示中文、切换后不更新。
- 修复收藏夹卡片按钮、操作菜单语言切换后不刷新。
- 修复导出弹窗、取色器弹窗中文未国际化。
- 修复相对时间显示（刚刚/分钟前/小时前/天前）未国际化。
- 修复英文 `messagesCountSuffix` 显示为键名本身。
- 修复设置页产品名和版本号丢失。
- 修复 `panel.html` 中空状态提示文字未添加 `data-i18n` 属性。
- 修复移动到文件夹弹窗匹配计数和"全部（无分类）"按钮未国际化。
- 修复收藏夹搜索计数条"共 X 项，匹配 Y 项"未国际化。
- 修复取色器弹窗标题双重图标问题。
- 修复隐私声明和安全提醒点击变半透明模糊。

## [1.3.0] - 2026-05-28

### 🏗️ 架构重构
- **面板代码模块化**：将原 `panel.js`（2140 行）拆分为 11 个独立模块，各司其职，极大降低维护难度。

### 🌐 国际化
- **全界面中英文切换**：覆盖 6 个标签页，约 120 个翻译键，语言切换即时生效，静态文本通过 `data-i18n` 统一刷新。
- 不支持的语言（俄语/印尼语等）提示"敬请期待"。

### 💾 存储监控
- 设置页新增存储用量卡片，实时显示百分比，超过 80% 橙色警告，超过 95% 红色警告。

### 🎨 UI 优化
- 关于页两列布局，字体渲染优化，空状态文字加亮，返回按钮深色模式可见。

### 🐛 修复
- 修复恢复默认设置按钮无反应、搜索排序按钮语言切换不更新、多个国际化遗漏等问题共十余项。

## [1.2.0] - 2026-05-23

### ✨ 新增
- 历史搜索刷新按钮（防并发锁）、导出 CSV（兼容 Excel）。
- 收藏夹搜索实时统计，范围覆盖文件夹名。
- 绝对时间显示开关（状态持久化）。
- 文件夹下拉菜单（搜索、高亮、滚动放大）。
- 移动到弹窗优化（搜索框、计数、滚动条美化）。
- 消息卡片标题自动追加序号（同对话多条）。
- 消息收藏独立统计条，今日/本周增量显示。
- 悬浮按钮长按菜单（打开面板/复制链接/切换尺寸）。
- 面板最小化到悬浮按钮（红点提示）。
- 预设尺寸切换（原/小/中/大，自动记忆）。
- 清空所有收藏（带二次确认）。
- 导出文件名加时间戳（精确到秒）。
- 侧边栏收起提示（≤3 条对话时）。
- 深海蓝主题优化。
- 面板边界保护，拖拽阈值防误触。

### 🐛 修复
- 修复标题栏误触拖拽、消息收藏定位错乱、面板小窗口显示不全、历史搜索缓存、清空收藏无响应、搜索过滤时统计不更新、深色主题文字不可见、工具栏按钮主题不跟随等多项问题。

## [1.1.1] - 2026-05-21

### ✨ 新增
- 收藏夹统计条（对话+消息计数，随文件夹切换更新）。
- 消息收藏备注（增删改查、搜索高亮）。
- 当前对话搜索结果一键复制。
- 面板打开自动聚焦搜索框。

### 🔧 优化 & 修复
- 搜索范围提示更清晰，导出/导入完整保留备注字段，移动文件夹后统计即时更新，修复删除后统计不刷新、标签切换聚焦失效等 bug。

## [1.1.0] - 2026-05-14

### ✨ 新增
- 收藏夹文件夹分类管理（新建/重命名/删除，项目自由移动）。
- Markdown 导出（带角色区分）。
- 悬浮按钮状态反馈（已收藏显示金色）。
- 面板位置按对话独立记忆。
- 匿名使用统计（GA4，不收集个人信息）。

### 🐛 修复
- 导出/导入只支持对话、未支持消息的问题，深色主题"关于"文字不可见，历史搜索时间显示异常，收藏卡片分隔线不明显，文件夹操作按钮缺少悬停提示。

## [1.0.0] - 2026-05-10

### 🎉 首个正式版本
- **双模式搜索**：历史对话检索 + 当前页面内容搜索，时间排序与红底白字高亮。
- **双重收藏**：对话收藏（悬浮按钮/快捷键/右键）与消息收藏（单条提问/回答）。
- **消息跳转**：收藏消息点击跳转回原对话并精准定位。
- **6 套主题** + 自定义取色器 + 跟随系统深色模式。
- **快捷键**：Alt+K 呼出面板，Ctrl+Shift+X 收藏。
- **数据管理**：JSON / TXT 导出备份与导入恢复。
- **收藏备注**：可添加自定义备注，搜索支持检索备注。
